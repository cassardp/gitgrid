{
  "name": "gitgrid",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "vite build && cd gitgrid-worker && npx wrangler deploy"
  },
  "devDependencies": {
    "vite": "^7.3.1"
  },
  "dependencies": {
    "lucide": "^0.577.0"
  }
}
